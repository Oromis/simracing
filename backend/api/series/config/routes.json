{
  "routes": [
    {
      "method": "GET",
      "path": "/series",
      "handler": "Series.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/series/count",
      "handler": "Series.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/series/:id",
      "handler": "Series.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/series",
      "handler": "Series.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/series/:id",
      "handler": "Series.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/series/:id",
      "handler": "Series.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
